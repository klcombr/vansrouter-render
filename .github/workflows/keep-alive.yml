name: Keep Render Alive

on:
  schedule:
    - cron: '*/14 * * * *'  # Every 14 minutes
  workflow_dispatch:  # Manual trigger

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping VansRouter
        run: |
          curl -sf https://vansrouter.onrender.com/health || echo "Service may be starting up"
