services:
  - type: web
    name: vansrouter
    runtime: docker
    plan: free
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: VANSROUTER_DATA_DIR
        value: /app/data
